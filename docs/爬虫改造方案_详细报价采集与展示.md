# 爬虫改造方案 - 详细报价采集与展示

## 一、背景与目标

### 现状问题
1. 当前爬虫只采集**基准价**（单一平均值），丢失了各供应商的具体报价信息
2. 不同纯度/规格的报价被平均后，波动信息被抹平，失去参考价值
3. 无法追踪具体哪个供应商/品牌在涨价或跌价

### 目标
实现**"展开前显示基准价，展开后显示详细报价"**的数据采集与前端展示方案：

```
展开前（表格行）:
┌────────┬────────┬───────┬──────────┐
│ 甲苯  │ 7800   │ 元/吨 │ ↑5.2%   │  ← 基准价（当日均值）
└────────┴────────┴───────┴──────────┘

展开后（展开行）:
┌─────────────────────────────────────────────────────────────┐
│ 详细报价（5家供应商）                            [基准价: 7800] │
├────────┬────────┬────────┬────────┬────────────────────────┤
│ 规格   │ 品牌   │ 单价   │ 交易商 │ 交货地                  │
├────────┼────────┼────────┼────────┼────────────────────────┤
│ 98%以上│ 国产   │ 7800   │ 南京润升│ 江苏省/南京市          │
│ 品级:优│ 中石化 │ 7800   │ ...    │ 江苏省                  │
│ ...    │ ...    │ ...    │ ...    │ ...                    │
└────────┴────────┴────────┴────────┴────────────────────────┘
```

---

## 二、页面结构分析

### 列表页结构（四个行业统一）

**URL Pattern**: `https://www.100ppi.com/mprice/plist-{category_id}-{industry_code}.html`

**表格选择器**: `table.lp-table.mb15`

**列结构（8列，完全一致）**:

| 列索引 | 列名 | 说明 | 示例（甲苯） |
|--------|------|------|-------------|
| 0 | 商品名称 | 产品名 | 甲苯 |
| 1 | 规格 | 详细规格（含分类标识） | `品级:优级品;` 或 `98%以上，无水` |
| 2 | 品牌/产地 | 供应商或产地 | 国产、中石化华东、进口 |
| 3 | 报价 | 单价（含单位） | `7800元/吨`、`984.88元/克` |
| 4 | 报价类型 | 市场价/出厂价 | 市场价、出厂价 |
| 5 | 交货地 | 地区 | 江苏省、山东省 |
| 6 | 交易商 | 供应商全称 | 南京润升石化有限公司VIP |
| 7 | 发布时间 | 日期 | 2026-05-21 |

**数据特点**:
- 每页包含**多日历史数据**（每行一个日期）
- 同一产品、同一日期有**多个供应商报价**
- 基准价 = 当日所有供应商报价的**算术平均值**

### 行业差异化字段（从规格列解析）

| 行业 | 规格格式 | 解析方式 |
|------|---------|---------|
| 农副（棕榈油） | `分类:食用精炼棕榈液油;熔点(℃):24;` | 提取"分类"和"熔点" |
| 能源（液化天然气） | `类别:液化天然气` | 提取"类别" |
| 有色（黄金） | `Au不小于(%):99.99` | 提取"Au不小"和纯度值 |
| 化工（甲苯） | `品级:优级品;` 或 `98%以上，无水` | 提取品级/纯度描述 |

---

## 三、数据存储方案

### price_records 表字段

**新增字段**: 无需修改表结构

**extra_data 字段结构**（JSON）:

```python
{
    # 行业通用字段
    "报价类型": "市场价",
    "基准价": 7800,  # 当日所有报价的算术平均值

    # 详细报价列表（用于展开显示）
    "详细报价": [
        {
            "规格": "98%以上，无水",
            "品牌": "国产",
            "单价": 7800,
            "单价原始": "7800元/吨",
            "交易商": "南京润升石化有限公司VIP",
            "交货地": "江苏省/南京市",
            "发布时间": "2026-05-21"
        },
        {
            "规格": "品级:优级品;",
            "品牌": "中石化华东",
            "单价": 7800,
            "单价原始": "7800元/吨",
            "交易商": "南京润升石化有限公司VIP",
            "交货地": "江苏省",
            "发布时间": "2026-05-21"
        },
        ...
    ],

    # 行业特有字段（保留用于参考）
    "化工特有": {"品级": "优级品", "纯度": "98%以上"},
    "能源特有": {"类别": "液化天然气", "有效时间": ""},
    "农副特有": {"分类": "食用精炼棕榈液油", "熔点": "24℃"},
    "有色特有": {"Au不小于": "99.99%", "品名": "黄金"}
}
```

### 计算逻辑

```python
def calculate_benchmark_and_details(rows: List[Dict]) -> Dict:
    """
    从表格行数据计算基准价和详细报价
    rows: 当日该产品的所有供应商报价行
    """
    prices = [row['单价'] for row in rows]
    benchmark = sum(prices) / len(prices) if prices else 0

    return {
        "基准价": round(benchmark, 2),
        "详细报价": rows
    }
```

---

## 四、改造步骤（分步实施）

---

### Step 1: URL 映射与数据库更新

**目标**: 将数据库中产品的 `source_url` 从详情页（detail-xxx.html）更新为列表页（plist-xxx.html）

**现状**: 当前数据库中 `source_url` 指向详情页 `https://www.100ppi.com/mprice/detail-{product_id}.html`

**目标 URL 格式**: `https://www.100ppi.com/mprice/plist-{category_id}-{industry_code}.html`

**操作**:

1. 在数据库产品管理页面（ProductManage.vue）或通过脚本批量更新 `products.source_url`
2. URL 映射规则:
   - 详情页: `https://www.100ppi.com/mprice/detail-{product_id}.html`
   - 列表页: `https://www.100ppi.com/mprice/plist-{category_id}-{industry_code}.html`
3. 其中 `industry_code` 为行业代码（如 `hg`=化工, `ny`=农副, `ny`=能源, `ys`=有色）
4. `category_id` 为该产品在列表页对应的分类 ID（需在生意社页面确定）

**验证**: 更新后查询确认 `source_url` 已指向 `plist-` 页面

---

### Step 2: 爬虫核心改造

**目标**: `shengyishe.py` 新增列表页解析 + 日期分组基准价计算

**改造文件**: `backend/scrapers/shengyishe.py`

#### 2.1 新增 `_parse_list_page()` 方法

```python
def _parse_list_page(self, page, industry: str) -> List[Dict]:
    """
    统一解析列表页表格（四个行业通用）
    返回: 当日所有报价行的解析结果
    """
    results = []
    try:
        table = page.query_selector('table.lp-table.mb15')
        if not table:
            return results

        rows = table.query_selector_all('tr')
        for row in rows:
            cells = row.query_selector_all('td')
            if len(cells) < 8:
                continue

            product_name = cells[0].text_content().strip()
            if not product_name or product_name == '商品名称':
                continue

            spec_text = cells[1].text_content().strip()     # 规格
            brand = cells[2].text_content().strip()          # 品牌/产地
            price_str = cells[3].text_content().strip()      # 报价
            price_type = cells[4].text_content().strip()     # 报价类型
            region = cells[5].text_content().strip()          # 交货地
            supplier = cells[6].text_content().strip()        # 交易商
            publish_date = cells[7].text_content().strip()    # 发布时间

            price = self.parse_price(price_str)
            if price is None or price <= 0:
                continue

            parsed_spec = self._parse_spec_text(spec_text, industry)

            results.append({
                'product_name': product_name,
                'spec_raw': spec_text,
                'brand': brand,
                'price': price,
                'price_str': price_str,
                'price_type': price_type,
                'region': region,
                'supplier': supplier,
                'publish_date': publish_date,
                'industry': industry,
                'parsed_spec': parsed_spec
            })
    except Exception as e:
        print(f"  列表页解析失败: {e}")

    return results
```

#### 2.2 新增 `_group_by_date_and_calculate_benchmark()` 方法

```python
def _group_by_date_and_calculate_benchmark(self, rows: List[Dict]) -> List[Dict]:
    """
    按日期分组，计算每日的基准价
    """
    from collections import defaultdict

    by_date = defaultdict(list)
    for row in rows:
        by_date[row['publish_date']].append(row)

    results = []
    for date, day_rows in by_date.items():
        prices = [r['price'] for r in day_rows]
        benchmark = sum(prices) / len(prices) if prices else 0

        results.append({
            'date': date,
            'benchmark': round(benchmark, 2),
            'quote_count': len(day_rows),
            'details': day_rows
        })

    return results
```

#### 2.3 新增 `_parse_spec_text()` 方法

```python
def _parse_spec_text(self, spec_text: str, industry: str) -> Dict:
    """
    从规格文本中解析行业特有信息
    """
    result = {}

    if industry == '农副':
        if '分类:' in spec_text:
            result['分类'] = spec_text.split('分类:')[1].split(';')[0]
        match = re.search(r'熔点\(℃\):([\d]+);?', spec_text)
        if match:
            result['熔点'] = match.group(1) + '℃'

    elif industry == '能源':
        if '类别:' in spec_text:
            result['类别'] = spec_text.split('类别:')[1]

    elif industry == '有色':
        match = re.search(r'Au不小于\(%\):([\d.]+)', spec_text)
        if match:
            result['纯度'] = match.group(1) + '%'
        result['品名'] = '黄金'

    elif industry == '化工':
        if '品级:' in spec_text:
            result['品级'] = spec_text.split('品级:')[1].rstrip(';')
        purity_match = re.search(r'(\d+\.?\d*%以上?)', spec_text)
        if not purity_match:
            purity_match = re.search(r'(\d+元以上)', spec_text)
        if purity_match:
            result['纯度描述'] = purity_match.group(1)

    return result
```

#### 2.4 新增 `_create_price_items()` 方法

```python
def _create_price_items(self, grouped_data: Dict, source_url: str, industry: str) -> List[Dict]:
    """
    从分组数据创建价格项
    """
    details = grouped_data['details']
    if not details:
        return []

    first = details[0]
    items = []
    for detail in details:
        item = {
            'name': first['product_name'],
            'specification': detail['spec_raw'],
            'brand': detail['brand'],
            'price': detail['price'],
            'price_str': detail['price_str'],
            'price_type': detail['price_type'],
            'region': detail['region'],
            'supplier': detail['supplier'],
            'date': detail['publish_date'],
            'unit': '元/吨' if industry in ['化工', '农副'] else ('元/立方米' if industry == '能源' else '元/克'),
            'price_category': '现货',
            'industry': industry,
            'source_url': source_url,
            'extra_data': {
                '报价类型': detail['price_type'],
                '基准价': grouped_data['benchmark'],
                '详细报价': details,
                '行业特有': first.get('parsed_spec', {})
            }
        }
        items.append(item)
    return items
```

#### 2.5 改造 `scrape_page()` 方法

将原来调用行业特定解析（`_parse_chemical_detail` 等），改为：

```python
def scrape_page(self, url: str) -> List[Dict]:
    industry = self._product_industry_map.get(url, "化工")
    print(f"  正在爬取 [{industry}]: {url}")

    results = []

    with sync_playwright() as p:
        browser = p.chromium.launch(headless=True)
        context = browser.new_context(
            user_agent='Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36',
            viewport={'width': 1920, 'height': 1080}
        )
        page = context.new_page()

        try:
            page.goto(url, timeout=30000, wait_until='networkidle')
            page.wait_for_timeout(1000)

            # 调用新的列表页解析
            rows = self._parse_list_page(page, industry)
            if rows:
                grouped = self._group_by_date_and_calculate_benchmark(rows)
                if grouped:
                    # 取最新一天的数据
                    latest = max(grouped, key=lambda x: x['date'])
                    return self._create_price_items(latest, url, industry)

            # fallback 到基准价解析
            price_data = self._parse_benchmark_price(page, industry)
            if price_data:
                return [price_data]

        except Exception as e:
            print(f"  页面加载失败: {e}")
        finally:
            browser.close()

    return results
```

#### 2.6 保留原有方法作为 Fallback

以下方法保留作为 fallback：
- `_parse_chemical_detail()`
- `_parse_energy_detail()`
- `_parse_agri_detail()`
- `_parse_nonferrous_detail()`
- `_parse_benchmark_price()`

---

### Step 3: 前端展开行改造

**目标**: Dashboard 展开行从显示"历史价格记录"改为显示"详细报价表格"

**改造文件**: `frontend/src/views/Dashboard.vue`

#### 3.1 展开列模板改造（第276-330行附近）

将展开内容从显示历史记录改为显示详细报价：

```html
<el-table-column type="expand" width="48">
  <template #default="{ row }">
    <div class="expand-content">
      <div class="expand-header">
        <p class="expand-title">
          详细报价（{{ row.extra_data?.详细报价?.length || 0 }}家供应商）
        </p>
        <span class="benchmark-badge">
          基准价: ¥{{ row.price?.toLocaleString() }}
        </span>
      </div>

      <!-- 详细报价表格 -->
      <el-table
        v-if="row.extra_data?.详细报价?.length"
        :data="row.extra_data.详细报价"
        size="small"
        class="detail-table"
      >
        <el-table-column prop="brand" label="品牌/产地" width="100" />
        <el-table-column prop="spec_raw" label="规格" width="120" show-overflow-tooltip />
        <el-table-column prop="price" label="单价" width="100">
          <template #default="{ row: detail }">
            <span class="price-value">¥{{ detail.price?.toLocaleString() }}</span>
          </template>
        </el-table-column>
        <el-table-column prop="price_type" label="报价类型" width="90" />
        <el-table-column prop="region" label="交货地" width="100" />
        <el-table-column prop="supplier" label="交易商" show-overflow-tooltip />
      </el-table>

      <!-- 无详细报价时显示提示 -->
      <div v-else class="no-detail-tip">
        暂无详细报价数据
      </div>
    </div>
  </template>
</el-table-column>
```

#### 3.2 行业列配置更新

更新 `industryColumns` 配置，对有色/农副行业使用 `spec_raw` 字段：

```javascript
// 在 industryColumns 中为各行业添加 spec_raw 映射
'有色': [
  { prop: 'product_name', label: '商品名称', minWidth: 120, useExtraData: '品名/纯度' },
  { prop: 'spec_raw', label: '规格', minWidth: 80 },  // 新增
  // ...
]
```

---

## 五、单位设置（按行业）

| 行业 | 单位 | 说明 |
|------|------|------|
| 化工 | 元/吨 | 默认 |
| 农副 | 元/吨 | 默认 |
| 能源 | 元/立方米 | 液化天然气按体积计价 |
| 有色 | 元/克 | 黄金按克计价 |

---

## 六、API 兼容性

### `/prices/latest` 响应（不变）

```json
{
  "product_id": 1,
  "product_name": "甲苯",
  "price": 7800,           // 基准价（当日均值）
  "unit": "元/吨",
  "trend": "涨",
  "change_percent": 5.2,
  "extra_data": {
    "基准价": 7800,
    "详细报价": [
      {"brand": "国产", "price": 7800, "spec_raw": "98%以上，无水", ...},
      {"brand": "中石化华东", "price": 7800, ...},
      ...
    ]
  }
}
```

### 兼容性说明

- 前端不展开时：**显示基准价**（price 字段），与原来完全一致
- 前端展开时：从 **extra_data.详细报价** 获取详细报价渲染
- 现有 `/prices/history/{product_id}` API 同样返回 extra_data，兼容展开显示

---

## 七、验收标准

### 功能验收

| 编号 | 验收项 | 合格标准 |
|------|--------|---------|
| V1 | URL映射正确 | 数据库中 source_url 指向 plist- 页面 |
| V2 | 爬虫能采集列表页 | 运行爬虫后，每个行业至少有数据入库 |
| V3 | 基准价计算正确 | 基准价 = 当日所有供应商报价的算术平均值（允许误差±0.01） |
| V4 | 详细报价完整 | extra_data.详细报价 包含当日所有供应商报价行 |
| V5 | 行业规格正确解析 | 农副解析出"分类"和"熔点"，有色解析出"纯度" |
| V6 | 单位设置正确 | 化工/农副=元/吨，能源=元/立方米，有色=元/克 |
| V7 | API 返回完整 | /prices/latest 返回的 extra_data 包含 基准价 和 详细报价 |
| V8 | 前端展开显示 | Dashboard 展开行能显示详细报价表格 |
| V9 | 前端不展开正常 | 不展开时显示基准价，与原功能一致 |

### 测试用例

#### TC1: 爬虫数据采集测试

```bash
# 启动爬虫
python -m backend.scrapers.shengyishe

# 检查数据库
sqlite3 procurement.db "SELECT product_name, price, unit, extra_data FROM price_records LIMIT 5;"
```

**预期结果**:
- price 字段 = 当日基准价
- extra_data 包含 "基准价" 和 "详细报价" 数组
- unit 正确（化工/农副=元/吨，能源=元/立方米，有色=元/克）

#### TC2: API 响应测试

```bash
# 调用 /prices/latest
curl "http://localhost:8000/api/v1/prices/latest?industry=化工"

# 检查返回的 extra_data
```

**预期结果**: 返回的 extra_data 结构：
```json
{
  "基准价": 7800,
  "详细报价": [
    {"brand": "国产", "price": 7800, ...},
    {"brand": "中石化华东", "price": 7800, ...}
  ]
}
```

#### TC3: 前端展开测试

1. 打开 Dashboard，选择"化工"行业
2. 找到甲苯产品，点击展开箭头
3. 检查展开行是否显示详细报价表格

**预期结果**:
- 展开行显示"详细报价（5家供应商）基准价: ¥7800"
- 表格显示5行详细数据（品牌、规格、单价、交易商等）

---

## 八、风险与注意事项

### 风险

| 风险 | 缓解措施 |
|------|---------|
| 页面结构变化 | 选择器使用 class 而非精确路径；增加备用选择器 |
| 网络超时 | 增加重试机制；设置合理的 timeout |
| 数据量变大 | 详细报价存入 extra_data（JSON），不影响主表性能 |

### 注意事项

1. **不要删除旧的行业解析方法** - 保留作为 fallback
2. **增量更新** - 只采集当日数据，不重复采集历史
3. **去重逻辑** - 按 product_id + record_date + supplier 去重
4. **日期格式** - 统一使用 YYYY-MM-DD 存入 extra_data.detailed_quotes

---

## 九、输出文件清单

完成改造后，以下文件应已更新：

1. **`backend/scrapers/shengyishe.py`** - 改造后的爬虫代码
2. **`frontend/src/views/Dashboard.vue`** - 改造后的前端（展开行显示）

---

*文档版本: v1.1*
*创建日期: 2026-05-21*
*状态: 待实施（分Step）*